# Running the code
### Initialize virtual environment. We used python `3.12`
```
uv sync
```
The environment should now be ready.
You can now select kernel in your python notebook editor of choice and run our code.